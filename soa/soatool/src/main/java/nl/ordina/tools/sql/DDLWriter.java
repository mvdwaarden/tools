package nl.ordina.tools.sql;

import metadata.MetaData;

public class DDLWriter {
	public StringBuilder writeDDL(MetaData data) {
		StringBuilder result = new StringBuilder();

//		for (MetaType type : data.getTypes()) {
//
//		}
		return result;
	}
}
