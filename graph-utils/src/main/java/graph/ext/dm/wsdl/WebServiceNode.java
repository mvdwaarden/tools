package graph.ext.dm.wsdl;

/**
 * Purpose: Represents a service node. A service node specifies which bindings
 * it provides (thus which interfaces can be consumed using which transport
 * protocols). Typically the service node also specifies an endpoint (entry
 * point)
 * 
 * @author mwa17610
 * 
 */
public class WebServiceNode extends WSDLNode {

}
